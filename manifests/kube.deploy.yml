apiVersion: v1
kind: Service
metadata:
  name: "cloudconfigserver_acc-service"
spec:
  # if your cluster supports it, uncomment the following to automatically create
  # an external load-balanced IP for the frontend service.
  #type: LoadBalancer
  type: NodePort
  ports:
  # the port that this service should serve on
  - name: http
    port: 8001

  selector:
    app: "cloudconfigserver_acc-selector"
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: "cloudconfigserver_acc-deployment"
spec:
  # this replicas value is default
  # modify it according to your case
  replicas: 1
  template:
    metadata:
      labels:
        app: "cloudconfigserver_acc-selector"
    spec:
      containers:
      - name: cloudconfigserver_acc
        image: cloudconfigserver_acc:latest
        readinessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 20
        # resources:
        #   requests:
        #     cpu: 100m
        #     memory: 100Mi
